# 改善因子信号分析工具

## 1. 角色设定
你现在是一个资深的 Python 自动化工程师。你的代码风格应该是：简洁、高健壮性、包含充分的注释，并且遵循 PEP 8 规范，变量及函数名应尽量使用完整的英文单词，单词之间使用下划线连接。

## 2. 任务设定
你完成了因子信号分析工具和配套的可视化UI的代码,目前,UI的显示存在下述的问题和缺陷,我希望你进行完善和修复:
	1. 收益分析中的累积收益图下标的日期显示格式错误,应改为yyyy-mm的形式,且每一个月都应该进行标注;
	2. 累计收益的收益累积时间似乎有问题,并不是每个月都进行累积,希望你进行检查并修复;
	3. 最好将收益用百分比方式显示,例如10.5%而不是0.105;
	4. 累计收益图表的标题存在错误,现无法根据选择的持仓方式进行显示,希望你进行修复;
	5. 月度收入柱状图的下标希望能够更加密集;
	6. 在因子信号统计中,并不需要month列和month_label列同时出现,只保留month_label列并前置即可;
	7. Daily IC图表存在与累积收益图标同样的问题,希望你进行检查并修复;
在这些需要修复的东西以外,我还需要你做下述修改:
	1. 我希望你修改隔日收益的计算方式,方程样例和说明会在"/dfs/data/tools/analyzer/compute_return.py"中,之后所有的隔日收益全部以这种方式计算,月收益与年收益以隔日收益的累积计算,取消不复权/前复权/后复权的选项;
	2. 取消Top-N等权多头的持仓方式,增加power的持仓方式,方法为对因子信号值先取rank,再进行demean+scale处理;
	3. 增加4个universe,分别为上证,深证,创业板,科创板,其成分可以从"/dfs/dataset/230-1724661625521/data/dataprod/BlockN/block4n.N,5860I"中获取,其为二维numpy数组,形状为(n_days,n_stocks),值为从0到3的整数值,0为上证,1为深证,2为创业板,3为科创板;
	4. 在月度表格中同时显示whole universe,HS300,创业板,科创板的收益率以进行对比;
	5. 在累计收益图表中,增加zz3000的基准收益作为对比项,zz3000的收益计算方式与因子信号相同,持仓方式以"/dfs/dataset/230-172466165521/data/dataprod/ZZ3000Index/ZZ3000.weight.N,5860f"中的数据进行因子信号的持仓计算方式进行处理后得到;
除此以外,我还有以下建议:
	1. 增加一个计算按键,以在选择信号数据和持仓方式后进行所有interval在whole universe上的所有计算,而后sub universe的数据可以从whole universe的计算结果中提取,这样可以增加同时显示的数据量以进行对比;
	2. 在月度收益表格中高亮前五收益的月;

## 3. 执行要求
1. 在写代码之前，请先检查本地是否有需要的第三方库（如 `pandas`），如果没有，请向我申请安装权限;
2. 文件使用的权限与编写分析工具时相同,你可以再度查看"/dfs/data/tools/analyzer/analyzer.md"以确认;
3. 你可以参考"/dfs/data/Prometheus-engine-so-new/prometheus/utils/cache_reader.py"中的代码以了解"/dfs/dataset/230-172466165521/data/dataprod/"中数据的提取方法,但不允许进行任何修改;